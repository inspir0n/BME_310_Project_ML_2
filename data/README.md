Drop these two files here, copied directly from your Kaggle notebook's
`artifacts/` folder, and the Results page fills itself in automatically:

    summary.json
    extended_metrics.csv

No renaming, no editing — the columns and keys already match what
app.py expects, because it's the exact format your training notebook
(Cell 19 / Cell 21) produces.
